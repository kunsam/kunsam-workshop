{
  "name": "kunsam-mini-game",
  "version": "1.0.4",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "tsc": "shx rm -rf lib && tsc -p tsconfig.json",
    "pub-npm": "npm run tsc && npm version patch -m 'Upgrade to %s for reasons' && npm publish"
  },
  "author": "kunsam",
  "license": "ISC",
  "devDependencies": {
    "shx": "^0.3.3",
    "typescript": "^4.1.3"
  }
}
