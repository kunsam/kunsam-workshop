{
  "name": "kunsam-mini-game",
  "version": "1.0.6",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "tsc": "shx rm -rf lib && tsc -p tsconfig.json",
    "cp-test": "cp -rf ./lib ../test_mini_game/node_modules/kunsam-mini-game/",
    "pub-test": "shx rm -rf lib && tsc -p tsconfig.json && npm run cp-test",
    "pub-npm": "npm run tsc && npm version patch -m 'Upgrade to %s for reasons' && npm publish"
  },
  "author": "kunsam",
  "license": "ISC",
  "devDependencies": {
    "shx": "^0.3.3",
    "typescript": "^4.1.3"
  }
}
